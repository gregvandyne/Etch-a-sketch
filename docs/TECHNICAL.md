# Technical documentation

Production website + CMS for Courtney Stockton Photography.

## Stack

- **Next.js 16** (App Router, TypeScript, static generation + tag-based on-demand revalidation)
- **Tailwind CSS 4** (design tokens in `src/app/globals.css` `@theme`)
- **Sanity v6** — content backend; Studio embedded at `/studio` (`next-sanity`)
- **Resend** — inquiry email delivery (plain REST call, no SDK)
- **Plausible** — optional cookieless analytics (enabled via env var)

No database, no custom auth, no servers to maintain: content lives in
Sanity's hosted datastore, images on Sanity's image CDN, and the site
deploys to any Node/Next host (Vercel is the assumed default).

## Repository layout

```
sanity.config.ts          Studio config (desk structure, singletons, templates)
sanity.cli.ts             Sanity CLI config (dataset import/export etc.)
scripts/generate-placeholders.mjs   Regenerates labeled placeholder SVGs
docs/                     This file, Courtney's guide, content audit, seed data
src/
  app/
    (site)/               Public pages (homepage, portfolio, blog, venues, …)
      [slug]/             Root catch-all: service-area pages, blog posts, CMS redirects
    studio/[[...tool]]/   Embedded Sanity Studio
    api/inquire           Inquiry form endpoint (validation, honeypot, rate limit, Resend)
    api/revalidate        Sanity publish webhook → revalidateTag
    api/draft-mode/*      Draft preview enable/disable
    sitemap.ts robots.ts  Generated from CMS content
  components/             Photo, GalleryViewer (lightbox), InquiryForm, Prose, …
  lib/
    content.ts            Content access layer (CMS or labeled sample fallback)
    sample-content.ts     Pre-credential sample content (verified copy, labeled placeholders)
    service-areas.ts      Local-SEO landing pages (preserved WordPress URLs)
    seo.ts site.ts        Metadata/JSON-LD helpers, canonical URL constants
    analytics.ts          Conversion events + content attribution
  sanity/
    schemaTypes/          gallery, venue, post(+category), testimonial, page singletons,
                          siteSettings, redirect, photograph/seo/blockContent objects
    structure.ts          Courtney-friendly desk structure
    lib/                  client, image URL builder, queries (GROQ), fetch wrapper
```

## Content architecture

Documents: `gallery` (category: wedding|engagement|family), `venue`, `post`,
`postCategory`, `testimonial`, singletons (`homePage`, `aboutPage`,
`experiencePage`, `investmentPage`, `contactPage`, `siteSettings`), `redirect`.

Relationships (all queried, never duplicated):
- gallery → venue; venue page reverse-queries its galleries and posts
- post → relatedGallery, relatedVenue, categories
- testimonial → relatedGallery
- homepage → featuredGalleries (falls back to `featured == true` galleries)

`src/lib/content.ts` is the only place pages read content from. When
`NEXT_PUBLIC_SANITY_PROJECT_ID` is unset, it serves the clearly-labeled
sample content from `sample-content.ts` (placeholders generated by
`scripts/generate-placeholders.mjs`). Once a project ID exists, sample
content is never mixed in — missing content renders as designed empty states.

## URL structure (preserves the previous WordPress site)

| Path | Content |
|---|---|
| `/`, `/galleries`, `/information`, `/contact`, `/blog` | same URLs as the old site |
| `/weddings`, `/engagements`, `/families` (+`/[slug]`) | new portfolio structure |
| `/venues`, `/venues/[slug]` | new venue architecture |
| `/[slug]` | catch-all: service-area pages, then blog posts (old root-level slugs), then CMS `redirect` documents, then 404 |
| `/category/[slug]` | preserved WordPress category archives (`weddings-2`, `couples`, …) |
| `/tag/*`, `/wp-content/*`, `/wp-admin/*` | hard redirects in `next.config.ts` |

Legacy blog posts keep their rankings by being recreated with their original
slugs (inventory in `docs/CONTENT-AUDIT.md`). As a safety net,
`src/lib/legacy-posts.json` lists every known legacy post URL: until a post
with that slug is published, the old URL issues a temporary (307) redirect
to its most relevant section instead of a 404, and `npm run seed:legacy`
generates `docs/seed/legacy-posts.ndjson` — one pre-filled CMS draft per
legacy post (title, original slug, category), so restoring a post is
"add photos, write story, publish." Anything else gets a permanent redirect
via the Redirects section in the Studio — no deploy needed. The blog index
paginates at 12 posts per page (`/blog`, `/blog/page/2`, …), and legacy
WordPress search URLs (`/?s=…`) redirect to `/blog`.

## Image pipeline

`src/components/Photo.tsx` renders plain `<img>` tags with a 7-step `srcset`
(480→2560w) built by `@sanity/image-url`: `auto=format` (AVIF/WebP), quality
80, focal-point-aware crops when an aspect ratio is forced, LQIP blur
backgrounds from asset metadata, explicit width/height (zero CLS), lazy
loading everywhere except priority heroes. The Next.js image optimizer is
deliberately unused — Sanity's CDN already does the work, with zero
serverless cost. Full-resolution originals are never served; the largest
derivative is 2560px.

## Caching & publishing

All CMS reads go through `sanityFetch` (`src/sanity/lib/fetch.ts`), which
tags every request (`gallery`, `post:slug`, …). Published pages are static
and cached indefinitely; the Sanity webhook posts to `/api/revalidate`
(HMAC-verified with `SANITY_REVALIDATE_SECRET`), which calls
`revalidateTag`. Publish-to-live is seconds, with zero rebuilds.

Draft preview: the Studio's **Preview** tab (Sanity Presentation tool)
iframes the site and enables Next.js draft mode through
`/api/draft-mode/enable` (`defineEnableDraftMode` from `next-sanity`,
validated server-side with `SANITY_API_READ_TOKEN` — no shared URL secret).
In draft mode `sanityFetch` queries the `drafts` perspective uncached, the
site shows a "Previewing drafts" pill, and `/api/draft-mode/disable` exits.

## Image migration from the previous site

`npm run migrate:images` crawls the previous WordPress site's blog posts
(the inventory in `src/lib/legacy-posts.json`), downloads the photographs,
uploads them to the Sanity asset store, and attaches them to the matching
pre-seeded draft posts: first image as the featured photograph, the rest
into the story body. Only empty fields are filled, so Studio edits are
never overwritten; re-runs are safe (downloads cached in `migration/`,
Sanity dedupes assets by hash).

Requires `SANITY_API_WRITE_TOKEN` (Editor token) in `.env.local`, and a
machine that can reach courtneystockton.com. Preview first with
`npm run migrate:images -- --dry-run --limit 3`. Caveat: these are the old
site's web-resolution derivatives, not camera originals; suitable as
interim content, with originals uploaded through the Studio over time.

## Instagram feed

`src/lib/instagram.ts` + `src/components/InstagramStrip.tsx` render the
latest Instagram photographs on the homepage, linking to the posts. The
section renders **only** when `INSTAGRAM_USER_ID` and
`INSTAGRAM_ACCESS_TOKEN` are set; otherwise it is absent (never faked).
The feed is fetched server-side from the Instagram Graph API and cached
for an hour (`revalidate: 3600`), so visitors never hit Instagram directly
and the token never reaches the browser.

Setup (owner action):
1. The Instagram account must be a professional account linked to a
   Facebook Page. Create a Meta app (Instagram Graph API), obtain a
   long-lived access token and the Instagram user ID.
2. Set both env vars in the host.
3. **Tokens expire after ~60 days.** Refresh with
   `GET https://graph.instagram.com/refresh_access_token?grant_type=ig_refresh_token&access_token=<token>`
   before expiry and update the env var (a monthly calendar reminder or a
   small scheduled job works). If that upkeep is unwanted, a managed feed
   service (e.g. Behold) can replace this with a stable feed URL; the
   fetch boundary in `src/lib/instagram.ts` is the only file to change.

Note: photos flow one way (Instagram → website). Publishing a gallery on
the website never posts to Instagram.

## Inquiry pipeline

`POST /api/inquire` → server-side validation → spam checks (honeypot field,
minimum fill time, 5/hour/IP in-memory rate limit) → Resend REST API with
`reply_to` set to the inquirer. If Resend env vars are missing in a real
deployment the API returns 503 with an honest message (it never fakes
success); in the pre-credential sample build it logs the inquiry server-side.

## Analytics

`src/lib/analytics.ts` — typed events (Inquiry CTA Clicked / Form Started /
Form Submitted, Portfolio/Gallery/Investment Viewed, Email/Social clicks)
sent to Plausible when `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` is set; no-ops
otherwise. Content attribution: the last content page viewed is kept in
sessionStorage (first-party, per-tab, no PII) and attached to inquiry events
and the inquiry email, so inquiries can be traced to the content that drove
them.

## SEO

- Unique titles/descriptions per page (`buildMetadata` in `src/lib/seo.ts`),
  canonical URLs, OpenGraph/Twitter cards with 1200×630 CDN-cropped images;
  pages without a photograph fall back to a branded sharing card rendered
  at `/og` (`next/og`)
- CMS `seo` object overrides on every content type; `noIndex` supported
- JSON-LD: ProfessionalService + Person sitewide, BlogPosting on posts,
  BreadcrumbList on galleries/venues/posts — generated from structured
  content, never hand-edited
- `sitemap.xml` and `robots.txt` generated from CMS content
- Security headers in `next.config.ts`

## Local development

```bash
npm install
cp .env.example .env.local   # fill in what you have; empty = sample-content mode
npm run dev                   # site on :3000, Studio on :3000/studio
npm run build && npm start    # production build
npm run lint
npm run qa                    # browser QA sweep against a running server
                              # (requires: npm install -D playwright)
```

`npm run qa` loads every route at mobile and desktop widths and checks
status codes, overflow, broken images/links, alt text, metadata, JSON-LD,
sitemap/robots/redirects, the inquiry form's success and validation states,
and keyboard focus. Screenshots land in `qa-shots/` (gitignored). Point it
at another environment with `QA_BASE_URL=…`.

## Deployment (Vercel or any Node host)

1. Create the Sanity project (sanity.io → new project; dataset `production`).
2. Set the environment variables from `.env.example` in the host.
3. Deploy (`next build` / framework preset). 
4. In sanity.io/manage → API: add the deployed origin to CORS origins
   (with credentials) so `/studio` can talk to the API; create the
   revalidate webhook (see `src/app/api/revalidate/route.ts` header comment);
   create a Viewer token for `SANITY_API_READ_TOKEN`.
5. Seed initial content: `npx sanity dataset import docs/seed/seed.ndjson production`
   and `npx sanity dataset import docs/seed/legacy-posts.ndjson production`
   (see `docs/seed/README.md`), then upload photographs in the Studio.
6. Point the courtneystockton.com DNS at the deployment.

Backups: `npx sanity dataset export production` produces a full content +
assets archive; Sanity also keeps document history natively.

## Notes / remaining owner actions

- Sanity, Resend, Plausible and hosting credentials must be created by the
  owner — see the final section of `.env.example`.
- Real photographs must be uploaded through the Studio; the repo ships only
  labeled placeholder art.
- Legacy blog post bodies could not be crawled from this build environment;
  recreate them in the Studio with their original slugs
  (`docs/CONTENT-AUDIT.md` has the full URL inventory).
- The GitHub repository is still named `Etch-a-sketch`; renaming it to
  `courtney-stockton-photography` is a Settings → General → Rename action by
  the repo owner (GitHub auto-redirects the old name).
